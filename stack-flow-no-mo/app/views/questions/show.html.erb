<div>
<h1><%= @question.title %></h1>
<h2>by <%=link_to @question.user.username, user_path(@question.user)%></h2>
Votes: <%=@question.votes.sum(:value)%>
<p>
  <%= @question.content%>
</p>
  <%= link_to "Upvote", q_upvote_question_votes_path(@question), method: 'post' %>
  <%= link_to "Downvote", q_downvote_question_votes_path(@question), method: 'post' %>
  <%@question.comments.each do |comment|%>
    <div>
      <fieldset>
        <legend>Comment by: <%=comment.user.username%></legend>
        <%=comment.content%>
      </fieldset>
    </div>
  <%end%>
</div>
<br>
<hr>
<br>
<%= render 'answer' %>

<%= form_for @answer, url: question_answers_path(@question) do |f| %>
<fieldset>
  <legend>Answer this question!</legend>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit %>
</fieldset>
<% end %>
